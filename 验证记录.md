# V3 验证记录

验证日期：2026-09-07。本机 Codex CLI：0.153.0-alpha.5。

## 真实接口验证

- Codex App Server 初始化：成功。
- 模型目录：成功读取当前账号 6 个可见模型、6 份模型简介及各自支持的推理强度。
- 八档自动分流：真实短任务被判定为“极速任务”，随后使用 gpt-5.6-luna / low 完成；分类理由与回答均正常返回。
- 原生历史：成功读取当前账号的根会话；随机打开一条 13 轮会话，用户消息和模型回答均完整还原。
- 账户剩余用量：成功读取 2 个独立限额窗口。
- 上下文与压缩：隔离的临时会话返回 170 / 258400 tokens；手动压缩请求成功，并收到 contextCompaction 开始和完成事件。
- MCP 初始化、认证与工具列表：已使用标准 Streamable HTTP MCP 验证成功。
- 从 Windows 当前用户加密缓存启动：成功；未继承当前任务中的 MCP 令牌环境变量时仍能连接。
- 自动难度判断、同一会话续聊、引擎重启后恢复、停止分类、本地 SSE、同源与 CSRF 防护：沿用第一版，均已验证。

## 自动检查

Node 测试覆盖八档分类约束、实时模型简介注入、模型及推理强度校验、原生历史懒加载和消息转换、多窗口用量归一化、上下文与压缩事件、多 MCP 隔离和认证边界、停止与并发控制、旧轮次通知隔离。

TypeScript 检查通过；Vite 生产构建通过。

## 范围

当前版本只支持一个活动任务，未知交互默认拒绝。没有对用户已有会话执行压缩；压缩联调使用独立的临时会话。没有执行记忆写入、删除或真实文件变更审批测试；对应拒绝路径使用模拟协议验证。

本版本使用当前本机 App Server 协议及实验性的 `respect_system_proxy`；Codex 更新后可能需要适配。原生 Codex 历史已接入，但这不等于原记忆服务网页的实时状态同步。
